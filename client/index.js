import "./style.scss"
import "./nengi.js"
import Phaser from 'phaser' // phaser imports itself globally so it only needs imported once, here, and then can be used in any file
import "./phaser/phaser.js"